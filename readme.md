## Practice and playing around with TypeScript syntax and different features. e.g basics, types, signatures, interfaces, generics. Bare bones site was used as a practice field.

## To launch:

1. npm init
2. clone repo
3. npm install -g typescript
4. follow typescript docs for compiling
